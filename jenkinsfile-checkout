pipeline{
    agent any
    stages{
        stage(checkout){
            steps{
                (checkout scmGit(
                    branches: [[name: '*/main']],
                     extensions: [],
                      userRemoteConfigs:
                       [[credentialsId: 'git', url:
                        'https://github.com/Karthik101292/jenkins-rr.git']]))
            }
        }
    }
}